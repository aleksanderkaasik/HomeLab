---
- name: Running Nginx Reverse Proxy Playbook
  hosts: nginx_reverse_proxy
  vars_files:
    - variables/config/nginx_reverse_proxy.yml
    - variables/system/certificate.yml
  tasks:
    - name: Installing nginx, certbot and nginx plugin
      become: true
      ansible.builtin.apt:
        name:
          - nginx
          - certbot
          - python3-certbot-nginx
        state: present
        update_cache: true

    - name: Enabling nginx
      become: true
      ansible.builtin.systemd_service:
        name: nginx
        state: started
        enabled: true

    - name: Deploy nginx config
      become: true
      ansible.builtin.copy:
        content: |
          server {
            listen 80;
            server_name {{ item.domain }};
            location / {
                proxy_pass {{ item.path }};
                include proxy_params;
            }
            {% for redirect in item.redirects | default([]) %}
            location = {{ redirect.from }} {
                return 301 https://$server_name/{{ redirect.to | default("") }};
            }
            {% endfor %}
          }
        dest: "/etc/nginx/sites-enabled/{{ item.domain }}.conf"
        owner: root
        group: root
        mode: "0644"
      loop: "{{ reverse_proxy_config }}"
      notify: Restart nginx

    - name: Generate SSL certificates
      become: true
      ansible.builtin.shell: certbot certonly --nginx -d {{ item.domain }} --non-interactive --agree-tos --register-unsafely-without-email
      args:
        creates: "/etc/letsencrypt/live/{{ item.domain }}/"
      loop: "{{ reverse_proxy_config }}"
      notify: Restart nginx   

    - name: Deploy nginx config
      become: true
      ansible.builtin.copy:
        content: |
          server {
            listen 80;
            server_name {{ item.domain }};
          {% if item.redirect_https_is_enabled %}
            return 301 https://$server_name$request_uri;
          {% else %}
            location / {
                proxy_pass {{ item.path }};
                include proxy_params;
            }
            {% for redirect in item.redirects | default([]) %}
            location = {{ redirect.from }} {
                return 301 https://$server_name/{{ redirect.to | default("") }};
            }
            {% endfor %}
          {% endif %}
          }
          
          server {
            listen 443 ssl http2;
            server_name {{ item.domain }};

            ssl_certificate /etc/letsencrypt/live/{{ item.domain }}/fullchain.pem;
            ssl_certificate_key /etc/letsencrypt/live/{{ item.domain }}/privkey.pem;

            {% for redirect in item.redirects | default([]) %}
            location = {{ redirect.from }} {
                return 301 https://$server_name/{{ redirect.to | default("") }};
            }
            {% endfor %}
            location / {
                proxy_pass {{ item.path }};
                include proxy_params;
            }
          }
        dest: "/etc/nginx/sites-enabled/{{ item.domain }}.conf"
        owner: root
        group: root
        mode: "0644"
      loop: "{{ reverse_proxy_config }}"
      notify: Restart nginx

    - name: Getting list of Certbot Certificates
      become: true
      ansible.builtin.shell: certbot certificates | grep Domains | tr -d ' ' | sed 's/Domains://g'
      register: CertbotCertificates

    - name: Delete unused Certbot Certificates
      become: true
      ansible.builtin.shell: certbot delete --cert-name {{ item }} --non-interactive --agree-tos --register-unsafely-without-email
      when: not item in (reverse_proxy_config | map(attribute='domain') | list)
      loop: "{{ CertbotCertificates.stdout_lines }}"

    - name: Getting list of Nginx site configs
      become: true
      ansible.builtin.shell: ls /etc/nginx/sites-enabled/
      register: nginxSites

    - name: Delete unused Nginx site configs 
      become: true
      ansible.builtin.file:
        state: "absent"
        path: "/etc/nginx/sites-enabled/{{ item }}"
      when: item in (reverse_proxy_config | map(attribute='domain') | list)
      loop: "{{ nginxSites.stdout_lines }}"

    - name: Configure automatic certificate renewal
      ansible.builtin.cron:
        name: "Certbot certificate renewal"
        minute: "0"
        hour: "3"
        day: "*"
        month: "*"
        weekday: "*"
        job: "/usr/bin/certbot renew --quiet --deploy-hook 'systemctl reload nginx'"

  handlers:
    - name: Restart nginx
      become: true
      ansible.builtin.systemd_service:
        name: nginx
        state: restarted
